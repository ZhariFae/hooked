import React from 'react';

const AuthContext = React.createContext();
export default AuthContext;
// Remove the useAuth export from here to avoid confusion,
// as auth/useAuth.js provides a more robust version.
